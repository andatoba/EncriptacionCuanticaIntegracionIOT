apiVersion: v1
kind: Service
metadata:
  name: dashboard-frontend
  namespace: quantum-access-control
spec:
  selector:
    app: dashboard-frontend
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
  type: NodePort
